[project]
name = "header-extractor"
version = "0.1.0"
description = "Extract and patch headers from source repositories"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pygit2>=1.18.0",
    "pyyaml>=6.0.2",
]
